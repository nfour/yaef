{
  "name": "yaef-monorepo",
  "version": "0.6.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "reinstall": "yarn oao clean && yarn",
    "build": "oao all --parallel --tree tsc",
    "test": "source ./.env && jest",
    "lint": "tslint packages/**/src/**/*.ts"
  },
  "workspaces": {
    "packages": [ "packages/*" ]
},
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.2",
    "jest": "^24.8.0",
    "oao": "^1.6.0",
    "ts-jest": "^24.0.2",
    "tslint": "^5.16.0",
    "tslint-config-temando": "^1.6.1"
  }
}
